# macOS
.DS_Store

# Original design handoff archive (the extracted source stays in design_handoff/)
design_handoff/handoff.tar.gz

# Editor / local
*.log

# Backend (Node)
server/node_modules/
server/.env
# SQLite + audios completos + clips (datos en runtime, se borran a los 21 días)
server/data/
